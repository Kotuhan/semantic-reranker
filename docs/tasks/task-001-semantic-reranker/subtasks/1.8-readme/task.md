# Subtask 1.8: README.md

## Parent Task
task-001-semantic-reranker

## Description
Create setup and run instructions: prerequisites (Python 3.10+), setup (venv + pip install), first-run model download note (~80MB, subsequent runs offline), run instructions (python main.py, python evaluate.py), project structure overview.

## Acceptance Criteria

* Given README.md
  When followed on a fresh Python 3.10+ environment
  Then the user can install deps, run main.py, and run evaluate.py successfully

* Given README.md
  When reviewed
  Then it documents the first-run model download requirement

## Verification Steps
- Prerequisites, setup, run instructions present
- Model download note included
- Project structure overview included

## Files to Create/Modify
- `apps/semantic-reranker/README.md`
